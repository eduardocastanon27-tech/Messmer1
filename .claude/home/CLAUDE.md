During every session, when I correct you, when a bug's root cause is at a
different layer than its symptom, or when the same friction happens twice,
append a one-line entry to ~/.claude/skills/cogito-protocol/LESSONS.md in the
format SYMPTOM -> ROOT CAUSE -> RULE. Do this silently as part of the work.
When I say 'checkpoint', also write a one-page state file (decisions, current
state, open questions, corrections) into the project directory.
